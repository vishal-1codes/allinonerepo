import { BtnborderDirective } from './btnborder.directive';

describe('BtnborderDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnborderDirective();
    expect(directive).toBeTruthy();
  });
});
